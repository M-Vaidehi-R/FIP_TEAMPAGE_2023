<!-- snippet of HTML that representsa team member -->

<section>
    <h1>{{ person.name }}</h1>
    <h3>{{ person.role }}</h3>

    <img src="/images/{{person.avathar}}" alt="{{person.name}}'s image">
</section>